
1. 入力仕様に関する不明点
ファイル形式制約
・GPXのみか（拡張子チェックするか／中身判定するか）
→入力はgpxのみである。
→拡張子をチェックと中身判定をせよ。判定の結果、エラーであればその旨を示したメッセージダイアログを表示し終了せよ。

・複数ファイル選択を許可するか（現状コードは単一ファイル前提）
→単一ファイル前提とする。

文字コード・異常GPX
・壊れたXMLの扱い（エラー表示のみか／スキップか）
→ダイアログでエラー表示をし終了せよ。
・namespaceあり／なし混在時の挙動は現状維持でよいか
→現状維持とせよ。

waypointが0件の場合
・空CSVを出力するか／エラー扱いか
→エラー扱いとし、その旨ダイアログで表示して終了せよ。

2. アンテナ高入力に関する不明点
入力形式
・数値のみ許可（float）
→浮動小数点のみを許可せよ。

・単位は固定でmか（UI上に明記するか）
→単位は固定でmである。UI上に明記せよ。

デフォルト値
・初期値を持たせるか（例：1.8mなど）
→1.8mを初期値とせよ。

・前回入力値の記憶は必要か
入力ファイル、アンテナ高ともに前回入力値を記憶せよ。

入力エラー処理
・数値以外入力時の扱い（入力不可にする／エラー表示）
→入力不可にせよ

3. 出力仕様に関する不明点
出力先
・「入力ファイルと同じディレクトリ」で確定か
→確定である。

・上書き時の扱い（既存CSVがある場合）
上書き
別名保存
確認ダイアログ
→確認ダイアログを表示し、ダイアログを消したあとに再入力できるようにせよ。

ファイル名
・元ファイル名.csvで固定か
→固定である。
・タイムスタンプ付与の要否
→付与せよ。

出力成功時の通知
ダイアログ表示か
パス表示のみか
フォルダを開くボタンを付けるか
→ダイアログをで成功した旨を表示し、「フォルダを開く」ボタンか「OK」ボタンでそのまま終了する。

4. UI構成に関する不明点

最低限要件は以下だが詳細未定：

入力ファイル選択（ファイルダイアログ）
アンテナ高入力

不足している可能性がある要素：

実行ボタン（必須か自動実行か）
→実行ボタンを実装せよ。

処理中表示（プログレスバー等）
→プログレスバーを表示せよ。

ログ表示領域の要否
→ログ表示領域を設けよ。

エラーメッセージ表示方法
→ダイアログでエラーの内容を表示せよ。エラーが起きた場合は処理を中断しOKボタン押下で終了せよ、

5. エラー処理・例外系
・ファイル未選択で実行した場合
→入力ファイルとアンテナ高への入力があった時点で実行ボタンをアクティブにせよ。
アンテナ高未入力で実行した場合
→入力ファイルとアンテナ高への入力があった時点で実行ボタンをアクティブにせよ。
.pyprojエラー（proj.db不整合など）
→エラーの内容をダイアログに表示してOKボタン押下で終了せよ。
・座標変換失敗時の扱い（スキップ or 全体停止）
→変換できなかったgpxファイルの行数をダイアログに表示し、OKボタン押下で終了せよ。
・書き込み失敗（権限・ディスク）
→エラーの内容をダイアログに表示してOKボタン押下で終了せよ。

6. 実行環境・配布形態
・対象OS：Windows 11で確定か
→Windows11で確定である。

・配布形式
Python実行前提
exe化（onefile）
→onfirにせよ。
ライブラリ依存（pyproj）の同梱方法
→どのような方法があるか提案せよ。

7. 処理仕様の確認（既存コードに依存）

以下を仕様として固定してよいか確認が必要：

座標系：EPSG:4326 → EPSG:6677（第9系）
Z値：ele - アンテナ高
測点名：
nameタグ優先
無い場合 P001 連番
DATE：
YYYYMMDD
無い場合空欄
→良い。

8. 操作フロー

未確定点：
ファイル選択 → 即実行か
ファイル選択 → アンテナ高入力 → 実行ボタン押下か
→ファイル選択 → アンテナ高入力 → 実行ボタン押下とせよ。

9. ユーザビリティ要件
・ドラッグ＆ドロップ対応の要否
→D&Dに対応せよ。
・キーボード操作対応
→対応せよ。
・複数回連続処理（状態保持）の要否
→要である。

10. 拡張余地（今決めるか保留か）
座標系選択（第9系固定でよいか）
出力CSVフォーマット変更
waypoint以外（track）の対応
→保留とする




追加の詰め
2. 未解消・矛盾・追加定義が必要な点
2.1 出力ファイル名の仕様衝突

「元ファイル名.csvで固定」
「タイムスタンプ付与せよ」

この2つは同時に成立しない。

要決定
A: filename.csv（固定）
B: filename_YYYYMMDDHHMMSS.csv
C: 上書き時のみタイムスタンプ付与
2.2 上書き時の挙動が不完全
→Aとせよ。


「確認ダイアログを表示し、ダイアログを消したあとに再入力」

不明点：

「いいえ」の場合の遷移が未定義
要決定
Yes → 上書き
No → ファイル選択画面へ戻る or 自動リネーム
Cancel → 処理中断
→これらすべてを実装せよ。


2.3 「終了」の定義が不明確

多くの箇所で：

「OKボタン押下で終了」

とあるが、以下が未定義：

アプリ自体を終了するのか
メイン画面に戻るのか

特に以下で矛盾：

「複数回連続処理（状態保持）」→要求あり
「エラー時終了」→毎回終了すると連続処理不可
要決定
エラー時：
A: アプリ終了
B: メイン画面に戻る（推奨）
→メイン画面に戻れ。メイン画面にアプリ終了ボタンを実装せよ。


2.4 プログレスバー仕様不足

現在：

「プログレスバーを表示せよ」

未定義：

determinate（進捗％表示）か
indeterminate（くるくる表示）か

GPX処理的には：
waypoint数をカウント可能 → determinate可能
→determinate（進捗％表示）とせよ。

2.5 ログ表示領域の粒度

未定義：

表示内容
ファイル読み込み
waypoint数
エラー行番号
保存要否（ログファイル出力）
→すべて表示せよ。処理中のWaypoint名（nameを）も表示せよ。

2.6 「変換できなかった行数」の定義

「変換できなかったgpxファイルの行数」

曖昧：

XML行番号か
waypoint単位か（実質こちら）

要定義
→ waypoint単位でカウントするのが妥当
→waypoint単位である。当該waipoint名を表示せよ。

2.7 D&D仕様の詳細不足

未定義：

・複数ファイルドロップ時
→複数ドロップは受け付けないことをダイアログで表示しOKボタン押下でメイン画面に戻る


・非GPXドロップ時
→非GPXであることをダイアログで表示しOKボタン押下でメイン画面に戻る。
ドロップ領域（全体 or 専用領域）
→メイン画面全体とする。

2.8 前回値記憶の保存方式

未定義：

保存場所
ini
json
レジストリ
保存タイミング
→iniディレクトリに生成せよ。iniディレクトリがない場合は作成せよ。json形式でよい。メイン画面のアプリを終了ボタンを押下したタイミングとする。

2.9 pyproj同梱方法（要求あり）

現時点では未回答要求：

主な選択肢
PyInstaller + hidden-import + データ同梱（標準）
proj.dbを明示的に同梱
conda-pack（非推奨）
embeddable Python + venv（複雑）

→別途提案可能
→別途提案せよ。

2.10 例外処理の一貫性

現状：

すべて「ダイアログ表示→終了」

ただし以下と衝突：

「連続処理（状態保持）」
→ダイアログ表示からメイン画面に戻るようにせよ。




さらなる不明点
2. 実装上の注意点（重要）
2.1 タイムスタンプ仕様の実質無効化

現在仕様では：

ファイル名固定
上書き確認あり

→ タイムスタンプが使われるケースが存在しない

実務上の選択肢（どちらか）
現状維持（仕様通り実装）
もしくは：
「No選択時にタイムスタンプ付きで保存」に変更
→現状維持とする。


2.2 「終了」系挙動の統一

すべて以下で統一されている：

成功 → ダイアログ → OK → メイン画面 or 終了
エラー → ダイアログ → OK → メイン画面

ただし：

成功時のみ「終了」挙動が曖昧
確認推奨
成功時もメイン画面に戻すのが一貫
→成功時もメイン画面に戻すようにせよ。


2.3 waypoint失敗時の仕様

現在仕様：

「失敗した件数を表示して終了」

しかし同時に：

ログに個別名表示

実質仕様
処理は継続（スキップ）
最後に件数表示

※これは重要（全停止ではない）
→「失敗した件数を表示して終了」は取りやめる。失敗した時点でダイアログに失敗したwaypoint名を表示して、OKボタン押下でメイン画面に戻るようにせよ。